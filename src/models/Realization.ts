export interface Realization {
    title: string;
    slug: string;
    client: string;
    image: ImageMetadata;
    category: string;
    description: string;
    date: string;
    testimonial?: string;
    author?: string;
    position?: string;
}